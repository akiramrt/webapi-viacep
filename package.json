{
  "name": "webapi-viacep",
  "version": "1.0.0",
  "description": "Comandos para executar o projeto:  - node -v  - npm -v  - npm init -y  - npm install -g json-server  - json-server --watch services.json --port 3000 ou npm run test-dev",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "test-dev": "node src/index.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.8.1",
    "express": "^4.21.2"
  }
}
